var NUMSTEPS = 8;

const keys = {
  BACKSPACE: 8
}

const circleAttrs = {
  DIAMETER: 60
}

const canvasAttrs = {
  HEIGHT: 600
}

const oscAttrs = {
  FADE_DURATION: 1,
  GAINSCALE: 400,
  FREQSCALE: 1,
}

const waveAbbrevs = {
  'sine': 'sin',
  'square': 'sqr',
  'triangle': 'tri'
}